import { RoleManagementDirective } from './role-management.directive';

describe('RoleManagementDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleManagementDirective();
    expect(directive).toBeTruthy();
  });
});
